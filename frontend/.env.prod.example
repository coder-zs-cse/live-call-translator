# Copy to .env.prod. Loaded explicitly by `npm run build:prod` / `start:prod`,
# never implicitly - pointing the panel at production has to be deliberate.
NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com
NEXT_PUBLIC_APP_ENV=prod
