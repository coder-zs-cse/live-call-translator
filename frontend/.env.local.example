# Copy to .env.local. Loaded automatically by `next dev`.
# NEXT_PUBLIC_ values are embedded in the browser bundle - never put a secret here.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_APP_ENV=local
